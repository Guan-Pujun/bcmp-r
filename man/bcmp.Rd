% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bcmp-generic.R
\name{bcmp}
\alias{bcmp}
\alias{bcmp,ANY-method}
\alias{bcmp,Seurat-method}
\alias{bcmp,SingleCellExperiment-method}
\title{Run BCMP from a supported single-cell object}
\usage{
bcmp(
  object,
  batch_key = "batch",
  assay = NULL,
  layer = NULL,
  partition_n_pcs = 30L,
  partition_n_hvg = 2000L,
  k_min = 3L,
  k_max = NULL,
  batch_frac_threshold_for_k_max = 0.01,
  min_batch_coverage = NULL,
  min_cells_in_domain_per_batch = 5L,
  max_underrepresentation_fold = 10L,
  max_residual_cell_frac = 0.05,
  exclude_cells = NULL,
  seed = 236L,
  output_level = "standard",
  verbose = TRUE
)
}
\arguments{
\item{object}{(\code{Seurat} / \code{SingleCellExperiment}) A Seurat object with an \code{Assay5}
assay, or a SingleCellExperiment object.}

\item{batch_key}{(\code{character}) Name of the batch metadata column in Seurat or
\code{colData}.}

\item{assay}{(\code{character} / \code{NULL}) Name of the raw-count assay. \code{NULL} uses the
default Seurat assay or \code{"counts"} for SingleCellExperiment.}

\item{layer}{(\code{character} / \code{NULL}) Name of the Seurat v5 raw-count layer, e.g.
\code{"counts"}. \code{NULL} selects \code{"counts"}. For SingleCellExperiment, leave this as
\code{NULL} and select the raw-count assay with \code{assay}.}

\item{partition_n_pcs}{(\code{integer}) Number of principal components used for BCMP.}

\item{partition_n_hvg}{(\code{integer}) Number of highly variable genes used for BCMP.}

\item{k_min}{(\code{integer}) Lower bound of the representation-scale search.}

\item{k_max}{(\code{integer} / \code{NULL}) Optional upper bound. \code{NULL} derives it from
batch sizes.}

\item{batch_frac_threshold_for_k_max}{(\code{numeric}) Batches at or below this global
cell fraction are ignored during automatic upper-bound derivation.}

\item{min_batch_coverage}{(\code{integer} / \code{NULL}) Minimum number of batches required
in each evaluated domain. \code{NULL} resolves to \code{max(2, ceiling(n_batches * 0.5))}.}

\item{min_cells_in_domain_per_batch}{(\code{integer}) Minimum effective cells required
for a batch to count as represented in a domain.}

\item{max_underrepresentation_fold}{(\code{numeric}) Maximum allowed fold
under-representation relative to global batch proportion.}

\item{max_residual_cell_frac}{(\code{numeric}) Maximum fraction of active cells that
may fall in residual domains excluded from the pass/fail decision.}

\item{exclude_cells}{(\code{character} / \code{factor} / \code{logical} / \code{NULL}) Cell IDs or a
logical mask in object cell order, where \code{TRUE} excludes a cell from
partitioning. Excluded cells remain in the returned object as
\verb{Excluded_<batch>}.}

\item{seed}{(\code{integer}) Random seed used by preprocessing, partitioning, and debug
UMAP.}

\item{output_level}{(\code{character}) \code{"standard"} (default) or \code{"debug"}.}

\item{verbose}{(\code{logical}) Emit progress records when \code{TRUE}.}
}
\value{
\code{"standard"} returns an \code{ObjectResult} with the copied \code{object},
\code{selection}, and \code{search_trace}. \code{"debug"} returns an \code{ObjectDebugResult},
which adds \code{debug}.
}
\description{
Run the complete BCMP workflow from raw counts in a Seurat v5 or
SingleCellExperiment object. BCMP selects highly variable genes within
batches, normalizes and scales the counts, computes an unintegrated PCA
representation, and runs the partition search.
}
